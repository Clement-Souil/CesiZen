{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --ignore-scripts && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve dist --listen 0.0.0.0:$PORT --single",
    "restartPolicyType": "ON_FAILURE"
  }
}
